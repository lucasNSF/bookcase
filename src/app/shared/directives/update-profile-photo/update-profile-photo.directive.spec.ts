import { UpdateProfilePhotoDirective } from './update-profile-photo.directive';

describe('UpdateProfilePhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateProfilePhotoDirective();
    expect(directive).toBeTruthy();
  });
});
