import { ChangePasswordVisibilityDirective } from './change-password-visibility.directive';

describe('ChangePasswordVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangePasswordVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
